import { Layout } from '@components/Layout';
import type { NextPage } from 'next';
import About from '@/components/Work';

const Page: NextPage = () => (
	<Layout>
		<About />
	</Layout>
);

export default Page;
